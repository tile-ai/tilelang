# Requirements to run local build with `--no-build-isolation` or other developments

apache-tvm-ffi>=0.1.11,<0.1.13
build
cmake>=3.26
cython>=3.1.0
ninja
packaging
pre-commit>=4.0.0
scikit-build-core
setuptools>=61
wheel
z3-solver>=4.13.0,<4.15.5; platform_system != 'Windows' or platform_machine != 'ARM64'
z3-solver>=4.15.8,<4.16; platform_system == 'Windows' and platform_machine == 'ARM64'

auditwheel; platform_system == 'Linux'
patchelf; platform_system == 'Linux'
delocate; platform_system == 'Darwin'
